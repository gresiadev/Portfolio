// Components 
import AppHookContainer from './AppHookContainer'

// Functions 
import { createRoot } from 'react-dom/client'

const root = createRoot(document.getElementById("root"))
root.render(<AppHookContainer />)

